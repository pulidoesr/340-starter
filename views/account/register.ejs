<% if (title) { %>
  <h1><%= title %></h1>
  <% } else {
    res.redirect('/')
  } %>

<%# messages() %>
<% if (messages.notice) { %>
  <div class="alert alert-danger">
    <%= messages.notice %>
  </div>
<% } %>


<%- registerPage %>